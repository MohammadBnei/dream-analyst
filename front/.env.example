# Replace with your DB credentials!
DATABASE_URL="postgres://user:password@host:port/db-name"

# Signs session cookies. REQUIRED — the app refuses to start without it, because
# a predictable signing key means anyone can forge a session for any user.
# Generate one with:  openssl rand -base64 48
JWT_SECRET=""
