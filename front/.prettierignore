# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/
/drizzle/

# VENDORED verbatim from MohammadBnei/ukubi-stt. Must stay byte-identical to
# upstream so re-copying is a straight file replace; formatting it here would
# produce a spurious diff on every sync.
src/lib/client/audio/stt-capture.js

# Generated by release-it/conventional-changelog. Reformatting causes conflicts
# on the next release.
CHANGELOG.md

# Generated by `paraglide-js compile` and gitignored, so they exist only after a
# build. Locally a `format` run makes lint pass; CI generates them fresh and
# fails on files nobody wrote. Checking generated output is not a style opinion.
project.inlang/.meta.json
project.inlang/README.md
src/lib/paraglide/
